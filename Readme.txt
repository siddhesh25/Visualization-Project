index.html inside the project folder can be run using XAMPP server.
All the standard graphs are visible through it.

For Non standard graphs, run the main.py file first and then navigate through d3 dashboard.